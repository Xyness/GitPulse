# Optional: GitHub Personal Access Token
# Without a token: 60 requests/hour (public API)
# With a token: 5,000 requests/hour
# Create one at: https://github.com/settings/tokens (no special scopes needed)
GITHUB_TOKEN=
